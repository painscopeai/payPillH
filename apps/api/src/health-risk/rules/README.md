# Machine-readable rule artefacts

Declarative thresholds live here for future tuning. Current scoring lives in `fallbackComposite.js` and `preventiveHints.js`; keep this folder aligned with [docs/health-risk-engine/RULE_CATALOG.md](../../../../docs/health-risk-engine/RULE_CATALOG.md) when weights change.

See `bp-bands.json` for NICE-oriented BP band references (documentation mirror).
