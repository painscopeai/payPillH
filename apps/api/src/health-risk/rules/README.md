# Machine-readable rule artefacts

Declarative thresholds live here for future extraction into the inference engine. Current releases encode the same logic in `fallbackComposite.js`, `preventiveHints.js`, and `qriskRunner.js`; keep this folder and [docs/health-risk-engine/RULE_CATALOG.md](../../../../docs/health-risk-engine/RULE_CATALOG.md) in sync when weights change.

See `bp-bands.json` for NICE-oriented BP band references (documentation mirror).
